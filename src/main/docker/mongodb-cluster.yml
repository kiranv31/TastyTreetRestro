version: '2'
services:
    tastytreetrestro-mongodb:
        image: mongo:3.4.10
        ports:
            - "27017:27017"
        command: mongos --configdb tastytreetrestro-mongodb-config
    tastytreetrestro-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    tastytreetrestro-mongodb-config:
        image: mongo:3.4.10
        container_name: tastytreetrestro-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
